---
title: 我的第一個Google Extensions套件 (中) - 頁面之間的溝通
date: 2022-02-17 10:22:19
tags: 外掛、套件
categories: Google extension
description: Google extension 運作關係，先來個簡易取得頁面內容
---
## 實際運作邏輯

套件主要的溝通方式主要會由三個js組成 background、conetent、popup，其餘js數量沒有限制
1. background : 長時間的待命，主要負責extension邏輯與功能，只要套件有開啟的狀態就是持續待命，簡單說不論你在哪一個分頁逛哪一個頁面，它都癡癡的等待你喚醒執行交代的工作
2. content : 專職跟頁面溝通的腳色，沒有它就無法取得頁面中的資料
3. popup : 使用者介面的呈現，可透過 extension API 讀取、操作 content.js

<br>

## 第二步：制作幕後主要溝通的頭頭 `background.js`

<br>
我們在 background.js 輸入 console.log 確認是否有成功載入

```
console.log('Info from background.js');
```

![](/img/0002/bkackground_test_2.jpg "套件載入後我們可以在檢視模式看到來自 background.js 的訊息了")

在這次設計套件中，主要我希望使用者在點擊套件後，會執行一個新的分頁，並且將之後需要的資料都集中在這個分頁中來操作，所以我們需要點擊以及開啟後的頁面內容，內容我們會寫在popup.html內。

```
chrome.action.onClicked.addListener((tabs) => {
  chrome.tabs.create({
    url: 'popup.html'
  });
});

```


## 第三步：使用者頁面 `popup.html`

我們先把使用者頁面內容製作起來

```html
<!DOCTYPE html>
<html lang="zh-TW">

<head>
  <meta charset="UTF-8">
  <title>popup頁面</title>
  <script defer src="./js/popup.js"></script>
</head>

<body>
  <div id="app">
    <h1>Popup 抬頭</h1>
    <button id="click_btn">Click</button>
    <div class="info"></div>
  </div>
</body>

</html>
```

刷新一下我們的套件看看有沒有成功

![](/img/0002/bkackground_test_3.jpg "1. 接著我們直接點刷新")
![](/img/0002/bkackground_test_4.jpg "2. 點套件圖式")
![](/img/0002/bkackground_test_5.jpg "3. 看到 Popup 抬頭 就代表我們成功開啟套件的內容頁面了")

## 第四步：抓目標頁面資訊 `content_scripts`

先回頭看先前設定的 manifest.json 
```json
  "content_scripts": [
    {
      "matches": [
        "*://www.google.com/"
      ],
      "js": [
        "./js/content.js"
      ],
      "run_at": "document_idle"
    }
  ]
```
1. matches : 符合的網址，可以替換任何想執行的網址
2. js : 執行的js檔案，js名稱可以自行取
3. run_at : 默認"document_idle"，試情況可更換"document_start" or "document_end"
   
<br>

接下來，我們的目標是要抓到 Google 首頁的 Logo， 顯示在我們的使用者頁面頁面內

![](/img/0002/bkackground_test_6.jpg)

<br>

1. 在 `content_scripts` 中可以透過 chrome.runtime.sendMessage 發送資料，希望在網頁完整讀取後就傳送
```js
//content_scripts 內容

window.onload = function () {
  //取得Google logo
  const googleLogoUrl = document.querySelector('.lnXdpd').src;

  chrome.runtime.sendMessage({
    url: googleLogoUrl,
  });
};
```

2. 在 `popup.js` 中可以透過 chrome.runtime.onMessage.addListener 接收資料，當 content_scripts 所加載的分頁完畢後就會傳入
```js
//popup.js 內容

const infoContent = document.querySelector('.info');

chrome.runtime.onMessage.addListener(function (request, sender, sendResponse) {
  infoContent.insertAdjacentHTML('beforebegin', `<img src="${request.url}">`);
});
```


![](/img/0002/result.gif "頁面加載後，使用者頁面出現LOGO代表我們成功取得分頁資料")



## 資料參考
[Getting started : https://developer.chrome.com/docs/extensions/mv3/getstarted/](https://developer.chrome.com/docs/extensions/mv3/getstarted/)